/**
 * Jest setup for Mock Service Worker (MSW)
 * Automatically starts and stops the mock server for HTTP request mocking
 */

// Import MSW utilities - we'll skip MSW setup for now due to import issues
// MSW can be set up manually in individual tests when needed

console.log('MSW setup skipped - can be configured manually in tests');